import React from "react";

const Slider = ({ data }) => {
  return (
    <div>
      <div className="slideShowContainer">
        <h2>{data}</h2>
      </div>
    </div>
  );
};

export default Slider;
